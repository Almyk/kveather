# kveather
Simple weather app using the kivy library

# How to use
You need to have the kivy library installed, find it at: https://github.com/kivy/kivy

Will also need to register for a free api key over at: https://openweathermap.org/api  
Store your key inside a file named "api.key".

Then to run the program simply type "python main.py".
